# agent-memory/

Long-term notes owned by individual sub-agents, one folder per agent, each with a `MEMORY.md` the
agent appends to across runs. One fact per entry.

Distinct from `memory/`, which is for humans and general project history.
