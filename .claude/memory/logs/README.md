# memory/logs/

Curated session/debug summaries worth keeping. Empty for now — the one session worth remembering is
written up as `memory/lineage/2026-07-rebuild.md` instead, since it's architectural history rather
than a debug log.
