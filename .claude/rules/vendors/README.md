# rules/vendors/

Empty on purpose. No third-party service integrations — the sims have no network calls, no auth,
no database, no payment or analytics SDKs. Dependencies are just pygame-ce, matplotlib and numpy,
which are covered by `rules/languages/python.md`.
