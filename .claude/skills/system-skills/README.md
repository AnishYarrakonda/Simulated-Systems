# skills/system-skills/

Empty on purpose. The only skill here is `verify/`, kept at the top level — categorising one item
adds nothing. Move skills into these folders once there are enough to need grouping.
